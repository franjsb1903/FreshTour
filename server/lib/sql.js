const sql = {};

sql.planificacions = {};
sql.planificacions.new = "INSERT INTO fresh_tour.planificacions (id_usuario, titulo, comentario, esta_compartida, valoracion, distancia, tempo_visita, tempo_ruta) VALUES ($1, $2, $3, $4, 0, $5, $6, $7) RETURNING id";
sql.planificacions.newLugares = "INSERT INTO fresh_tour.planificacions_lugares_turisticos (id_planificacion, id_lugar_turistico, posicion_visita, tipo_visita) VALUES ";
sql.planificacions.newMonumentos = "INSERT INTO fresh_tour.planificacions_monumentos (id_planificacion, id_monumento, posicion_visita, tipo_visita) VALUES ";
sql.planificacions.newTempoLugares = "INSERT INTO fresh_tour.tempo_lugares_turisticos (id_lugar_turistico, tempo, data) VALUES ($1, $2, CURRENT_TIMESTAMP)";
sql.planificacions.getAVGTempoLugares = "SELECT avg(tempo) as tempo FROM fresh_tour.tempo_lugares_turisticos WHERE id_lugar_turistico = $1";
sql.planificacions.updateTempoLugares = "UPDATE fresh_tour.lugares_turisticos SET tempo_visita_usuario = $1 WHERE id = $2";
sql.planificacions.newTempoMonumentos = "INSERT INTO fresh_tour.tempo_monumentos (id_monumento, tempo, data) VALUES ($1, $2, CURRENT_TIMESTAMP)";
sql.planificacions.getAVGTempoMonumentos = "SELECT avg(tempo) as tempo FROM fresh_tour.tempo_monumentos WHERE id_monumento = $1";
sql.planificacions.updateTempoMonumentos = "UPDATE fresh_tour.monumentos SET tempo_visita_usuario = $1 WHERE id = $2";
sql.planificacions.newHospedaxe = "INSERT INTO fresh_tour.planificacions_lugares_hospedaxe (id_planificacion, id_lugar_hospedaxe, posicion_visita, duracion) VALUES ";
sql.planificacions.newHostalaria = "INSERT INTO fresh_tour.planificacions_lugares_hostalaria (id_planificacion, id_lugar_hostalaria, posicion_visita, duracion) VALUES ";
sql.planificacions.newTempoHospedaxe = "INSERT INTO fresh_tour.tempo_lugares_hospedaxe (id_lugar_hospedaxe, tempo, data) VALUES ($1, $2, CURRENT_TIMESTAMP)";
sql.planificacions.newTempoHostalaria = "INSERT INTO fresh_tour.tempo_lugares_hostalaria (id_lugar_hostalaria, tempo, data) VALUES ($1, $2, CURRENT_TIMESTAMP)";
sql.planificacions.newTempoOcio = "INSERT INTO fresh_tour.tempo_actividades_ocio (id_actividade_ocio, tempo, data) VALUES ($1, $2, CURRENT_TIMESTAMP)";
sql.planificacions.newOcio = "INSERT INTO fresh_tour.planificacions_actividades_ocio (id_planificacion, id_actividade_ocio, posicion_visita, duracion) VALUES ";
sql.planificacions.newOutras = "INSERT INTO fresh_tour.planificacions_outras_actividades (id_planificacion, id_outra_actividade, posicion_visita, duracion) VALUES ";
sql.planificacions.newTempoOutras = "INSERT INTO fresh_tour.tempo_outras_actividades (id_outra_actividade, tempo, data) VALUES ($1, $2, CURRENT_TIMESTAMP)";
sql.planificacions.all = "SELECT *, (SELECT usuario FROM fresh_tour.usuarios u WHERE id = p.id_usuario) AS usuario, $2 AS id_actual_usuario, 'Planificación' AS tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.planificacions_favoritas pf WHERE pf.id_planificacion = p.id AND pf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.planificacions p WHERE esta_compartida = true ORDER BY valoracion DESC"
sql.planificacions.share = "UPDATE fresh_tour.planificacions SET esta_compartida = $1 WHERE id = $2";
sql.planificacions.elementos = "(SELECT *, 'Lugar turístico' AS tipo, (SELECT tipo_visita FROM fresh_tour.planificacions_lugares_turisticos plt WHERE plt.id_lugar_turistico = lt.id AND plt.id_planificacion = $1) as tipo_visita, (SELECT posicion_visita FROM fresh_tour.planificacions_lugares_turisticos plt WHERE plt.id_lugar_turistico = lt.id AND plt.id_planificacion = $1) as posicion_visita FROM fresh_tour.lugares_turisticos lt WHERE lt.id IN (SELECT id_lugar_turistico FROM fresh_tour.planificacions_lugares_turisticos plt WHERE plt.id_planificacion = $1) UNION all SELECT *, 'Monumento' AS tipo, (SELECT tipo_visita FROM fresh_tour.planificacions_monumentos pm WHERE pm.id_monumento = m.id AND pm.id_planificacion = $1) as tipo_visita, (SELECT posicion_visita FROM fresh_tour.planificacions_monumentos pm WHERE pm.id_monumento = m.id AND pm.id_planificacion = $1) as posicion_visita FROM fresh_tour.monumentos m WHERE m.id IN (SELECT id_monumento FROM fresh_tour.planificacions_monumentos pm WHERE pm.id_planificacion = $1)) ORDER BY posicion_visita";
sql.planificacions.lecer = "(SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, (SELECT duracion FROM fresh_tour.planificacions_lugares_hospedaxe plh WHERE plh.id_lugar_hospedaxe = lh.id AND plh.id_planificacion = $1) as tipo_visita, (SELECT posicion_visita FROM fresh_tour.planificacions_lugares_hospedaxe plh WHERE plh.id_lugar_hospedaxe = lh.id AND plh.id_planificacion = $1) as posicion_visita FROM fresh_tour.lugares_hospedaxe lh WHERE lh.id IN (SELECT id_lugar_hospedaxe FROM fresh_tour.planificacions_lugares_hospedaxe plh WHERE plh.id_planificacion = $1) UNION ALL SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, (SELECT duracion FROM fresh_tour.planificacions_lugares_hostalaria plh WHERE plh.id_lugar_hostalaria = lh.id AND plh.id_planificacion = $1) as tipo_visita, (SELECT posicion_visita FROM fresh_tour.planificacions_lugares_hostalaria plh WHERE plh.id_lugar_hostalaria = lh.id AND plh.id_planificacion = $1) as posicion_visita FROM fresh_tour.lugares_hostalaria lh WHERE lh.id IN (SELECT id_lugar_hostalaria FROM fresh_tour.planificacions_lugares_hostalaria plh WHERE plh.id_planificacion = $1) UNION ALL SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, (SELECT duracion FROM fresh_tour.planificacions_actividades_ocio pao WHERE pao.id_actividade_ocio = ao.id AND pao.id_planificacion = $1) as tipo_visita, (SELECT posicion_visita FROM fresh_tour.planificacions_actividades_ocio pao WHERE pao.id_actividade_ocio = ao.id AND pao.id_planificacion = $1) as posicion_visita FROM fresh_tour.actividades_ocio ao WHERE ao.id IN (SELECT id_actividade_ocio FROM fresh_tour.planificacions_actividades_ocio pao WHERE pao.id_planificacion = $1) UNION ALL SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, (SELECT duracion FROM fresh_tour.planificacions_outras_actividades poa WHERE poa.id_outra_actividade = oa.id AND poa.id_planificacion = $1) as tipo_visita, (SELECT posicion_visita FROM fresh_tour.planificacions_outras_actividades poa WHERE poa.id_outra_actividade = oa.id AND poa.id_planificacion = $1) as posicion_visita FROM fresh_tour.outras_actividades oa WHERE oa.id IN (SELECT id_outra_actividade FROM fresh_tour.planificacions_outras_actividades poa WHERE poa.id_planificacion = $1)) ORDER BY posicion_visita"
sql.planificacions.delete = {}
sql.planificacions.delete.lugares = "DELETE FROM fresh_tour.planificacions_lugares_turisticos WHERE id_planificacion = $1";
sql.planificacions.delete.monumentos = "DELETE FROM fresh_tour.planificacions_monumentos WHERE id_planificacion = $1";
sql.planificacions.delete.hospedaxes = "DELETE FROM fresh_tour.planificacions_lugares_hospedaxe WHERE id_planificacion = $1";
sql.planificacions.delete.hostalaria = "DELETE FROM fresh_tour.planificacions_lugares_hostalaria WHERE id_planificacion = $1";
sql.planificacions.delete.ocio = "DELETE FROM fresh_tour.planificacions_actividades_ocio WHERE id_planificacion = $1";
sql.planificacions.delete.outras = "DELETE FROM fresh_tour.planificacions_outras_actividades WHERE id_planificacion = $1";
sql.planificacions.delete.planificacion = "DELETE FROM fresh_tour.planificacions WHERE id = $1";
sql.planificacions.delete.comentarios = "DELETE FROM fresh_tour.comentarios_valoracions_planificacions WHERE id_planificacion = $1";
sql.planificacions.delete.favoritas = "DELETE FROM fresh_tour.planificacions_favoritas WHERE id_planificacion = $1";
sql.planificacions.edit = "UPDATE fresh_tour.planificacions SET titulo = $1, comentario = $2 WHERE id = $3";
sql.planificacions.sortBy = {};
sql.planificacions.sortBy.valoracion = "SELECT *, (SELECT usuario FROM fresh_tour.usuarios u WHERE id = p.id_usuario) AS usuario, $2 AS id_actual_usuario, 'Planificación' AS tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.planificacions_favoritas pf WHERE pf.id_planificacion = p.id AND pf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.planificacions p WHERE esta_compartida = true ORDER BY valoracion DESC";
sql.planificacions.sortBy.menorDistancia = "SELECT *, (SELECT usuario FROM fresh_tour.usuarios u WHERE id = p.id_usuario) AS usuario, $2 AS id_actual_usuario, 'Planificación' AS tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.planificacions_favoritas pf WHERE pf.id_planificacion = p.id AND pf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.planificacions p WHERE esta_compartida = true ORDER BY distancia ASC";
sql.planificacions.sortBy.maiorDistancia = "SELECT *, (SELECT usuario FROM fresh_tour.usuarios u WHERE id = p.id_usuario) AS usuario, $2 AS id_actual_usuario, 'Planificación' AS tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.planificacions_favoritas pf WHERE pf.id_planificacion = p.id AND pf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.planificacions p WHERE esta_compartida = true ORDER BY distancia DESC";
sql.planificacions.sortBy.maiorTempoVisita = "SELECT *, (SELECT usuario FROM fresh_tour.usuarios u WHERE id = p.id_usuario) AS usuario, $2 AS id_actual_usuario, 'Planificación' AS tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.planificacions_favoritas pf WHERE pf.id_planificacion = p.id AND pf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.planificacions p WHERE esta_compartida = true ORDER BY tempo_visita DESC";
sql.planificacions.sortBy.menorTempoVisita = "SELECT *, (SELECT usuario FROM fresh_tour.usuarios u WHERE id = p.id_usuario) AS usuario, $2 AS id_actual_usuario, 'Planificación' AS tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.planificacions_favoritas pf WHERE pf.id_planificacion = p.id AND pf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.planificacions p WHERE esta_compartida = true ORDER BY tempo_visita ASC";
sql.planificacions.sortBy.maiorTempoRuta = "SELECT *, (SELECT usuario FROM fresh_tour.usuarios u WHERE id = p.id_usuario) AS usuario, $2 AS id_actual_usuario, 'Planificación' AS tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.planificacions_favoritas pf WHERE pf.id_planificacion = p.id AND pf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.planificacions p WHERE esta_compartida = true ORDER BY tempo_ruta DESC";
sql.planificacions.sortBy.menorTempoRuta = "SELECT *, (SELECT usuario FROM fresh_tour.usuarios u WHERE id = p.id_usuario) AS usuario, $2 AS id_actual_usuario, 'Planificación' AS tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.planificacions_favoritas pf WHERE pf.id_planificacion = p.id AND pf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.planificacions p WHERE esta_compartida = true ORDER BY tempo_ruta ASC";
sql.planificacions.sortBy.menorTempoTotalRuta = "SELECT *, (SELECT usuario FROM fresh_tour.usuarios u WHERE id = p.id_usuario) AS usuario, $2 AS id_actual_usuario, 'Planificación' AS tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.planificacions_favoritas pf WHERE pf.id_planificacion = p.id AND pf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.planificacions p WHERE esta_compartida = true ORDER BY (tempo_ruta+tempo_visita) ASC";
sql.planificacions.sortBy.maiorTempoTotalRuta = "SELECT *, (SELECT usuario FROM fresh_tour.usuarios u WHERE id = p.id_usuario) AS usuario, $2 AS id_actual_usuario, 'Planificación' AS tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.planificacions_favoritas pf WHERE pf.id_planificacion = p.id AND pf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.planificacions p WHERE esta_compartida = true ORDER BY (tempo_ruta+tempo_visita) DESC";
sql.planificacions.fav = {};
sql.planificacions.fav.sortBy = {};
sql.planificacions.fav.sortBy.valoracion = "SELECT *,'Planificación' as tipo, true AS favorito, $2 AS id_actual_usuario FROM fresh_tour.planificacions p2 WHERE id IN ( SELECT id_planificacion FROM fresh_tour.planificacions_favoritas pf WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.planificacions.fav.sortBy.menorDistancia = "SELECT *,'Planificación' as tipo, true AS favorito, $2 AS id_actual_usuario FROM fresh_tour.planificacions p2 WHERE id IN ( SELECT id_planificacion FROM fresh_tour.planificacions_favoritas pf WHERE id_usuario = $1) ORDER BY distancia ASC";
sql.planificacions.fav.sortBy.maiorDistancia = "SELECT *,'Planificación' as tipo, true AS favorito, $2 AS id_actual_usuario FROM fresh_tour.planificacions p2 WHERE id IN ( SELECT id_planificacion FROM fresh_tour.planificacions_favoritas pf WHERE id_usuario = $1) ORDER BY distancia DESC";
sql.planificacions.fav.sortBy.maiorTempoVisita = "SELECT *,'Planificación' as tipo, true AS favorito, $2 AS id_actual_usuario FROM fresh_tour.planificacions p2 WHERE id IN ( SELECT id_planificacion FROM fresh_tour.planificacions_favoritas pf WHERE id_usuario = $1) ORDER BY tempo_visita DESC";
sql.planificacions.fav.sortBy.menorTempoVisita = "SELECT *,'Planificación' as tipo, true AS favorito, $2 AS id_actual_usuario FROM fresh_tour.planificacions p2 WHERE id IN ( SELECT id_planificacion FROM fresh_tour.planificacions_favoritas pf WHERE id_usuario = $1) ORDER BY tempo_visita ASC";
sql.planificacions.fav.sortBy.maiorTempoRuta = "SELECT *,'Planificación' as tipo, true AS favorito, $2 AS id_actual_usuario FROM fresh_tour.planificacions p2 WHERE id IN ( SELECT id_planificacion FROM fresh_tour.planificacions_favoritas pf WHERE id_usuario = $1) ORDER BY tempo_ruta DESC";
sql.planificacions.fav.sortBy.menorTempoRuta = "SELECT *,'Planificación' as tipo, true AS favorito, $2 AS id_actual_usuario FROM fresh_tour.planificacions p2 WHERE id IN ( SELECT id_planificacion FROM fresh_tour.planificacions_favoritas pf WHERE id_usuario = $1) ORDER BY tempo_ruta ASC";
sql.planificacions.fav.sortBy.menorTempoTotalRuta = "SELECT *,'Planificación' as tipo, true AS favorito, $2 AS id_actual_usuario FROM fresh_tour.planificacions p2 WHERE id IN ( SELECT id_planificacion FROM fresh_tour.planificacions_favoritas pf WHERE id_usuario = $1) ORDER BY (tempo_ruta+tempo_visita) ASC";
sql.planificacions.fav.sortBy.maiorTempoTotalRuta = "SELECT *,'Planificación' as tipo, true AS favorito, $2 AS id_actual_usuario FROM fresh_tour.planificacions p2 WHERE id IN ( SELECT id_planificacion FROM fresh_tour.planificacions_favoritas pf WHERE id_usuario = $1) ORDER BY (tempo_ruta+tempo_visita) DESC";
sql.planificacions.get = "SELECT *, (SELECT usuario FROM fresh_tour.usuarios u WHERE id = p.id_usuario) AS usuario, $2 AS id_actual_usuario, 'Planificación' AS tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.planificacions_favoritas pf WHERE pf.id_planificacion = p.id AND pf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.planificacions p WHERE esta_compartida = true and (p.id in (select id_planificacion from fresh_tour.planificacions_lugares_turisticos plt where plt.id_lugar_turistico in (select id from fresh_tour.lugares_turisticos where lower(titulo) like lower($2)) or p.id in (select id_planificacion from fresh_tour.planificacions_monumentos pm where pm.id_monumento in (select id from fresh_tour.monumentos where lower(titulo) like lower($2)) or lower(p.titulo) like lower($2)) ORDER BY valoracion DESC"

sql.usuarios = {};
sql.usuarios.edit = "UPDATE fresh_tour.usuarios SET usuario=$1, nome=$2, apelidos=$3, email=$4, contrasinal=$5 WHERE id=$6 RETURNING usuario, nome, apelidos, email, to_char(data, 'DD-MM-YY') as data";
sql.usuarios.exists = "SELECT usuario, email FROM fresh_tour.usuarios WHERE usuario LIKE $1 OR email LIKE $2";
sql.usuarios.delete = {};
sql.usuarios.delete.lugaresFav = "DELETE FROM fresh_tour.lugares_turisticos_favoritos WHERE id_usuario = $1";
sql.usuarios.delete.monumentosFav = "DELETE FROM fresh_tour.monumentos_favoritos WHERE id_usuario = $1";
sql.usuarios.delete.planificacionsFav = "DELETE FROM fresh_tour.planificacions_favoritas WHERE id_usuario = $1";
sql.usuarios.delete.comentariosLugares = "DELETE FROM fresh_tour.comentarios_valoracions_lugares_turisticos WHERE id_usuario = $1 RETURNING id_lugar_turistico";
sql.usuarios.delete.comentariosMonumentos = "DELETE FROM fresh_tour.comentarios_valoracions_monumentos WHERE id_usuario = $1 RETURNING id_monumento";
sql.usuarios.delete.comentariosPlanificacions = "DELETE FROM fresh_tour.comentarios_valoracions_planificacions WHERE id_usuario = $1";
sql.usuarios.delete.planificacionsId = "SELECT id FROM fresh_tour.planificacions WHERE id_usuario = $1";
sql.usuarios.delete.user = "DELETE FROM fresh_tour.usuarios WHERE id = $1";
sql.usuarios.new = "INSERT INTO fresh_tour.usuarios (usuario, nome, apelidos, email, contrasinal, data) VALUES ($1, $2, $3, $4, $5, CURRENT_TIMESTAMP) RETURNING id, usuario, nome, apelidos, email, to_char(data, 'DD-MM-YY') as data";
sql.usuarios.get = {};
sql.usuarios.get.user = "SELECT id, usuario, nome, apelidos, email, contrasinal, to_char(data, 'DD-MM-YY') as data FROM fresh_tour.usuarios WHERE usuario LIKE $1 OR email LIKE $1";
sql.usuarios.get.byId = "SELECT id, usuario, nome, apelidos, email, to_char(data, 'DD-MM-YY') as data FROM fresh_tour.usuarios WHERE id = $1";
sql.usuarios.get.elementosFav = "SELECT *, 'Monumento' as tipo, true AS favorito FROM fresh_tour.monumentos m WHERE id IN ( SELECT id_monumento FROM fresh_tour.monumentos_favoritos mf WHERE id_usuario = $1) UNION ALL SELECT *, 'Lugar turístico' as tipo, true AS favorito FROM fresh_tour.lugares_turisticos lt WHERE id IN ( SELECT id_lugar_turistico FROM fresh_tour.lugares_turisticos_favoritos ltf WHERE id_usuario = $1)";
sql.usuarios.get.plansFav = "SELECT *, (SELECT usuario FROM fresh_tour.usuarios u WHERE id = p2.id_usuario) AS usuario, 'Planificación' as tipo, true AS favorito FROM fresh_tour.planificacions p2 WHERE id IN ( SELECT id_planificacion FROM fresh_tour.planificacions_favoritas pf WHERE id_usuario = $1)";
sql.usuarios.get.opinions = "SELECT id, id_usuario, titulo, to_char(data, 'DD-MM-YY') as data, valoracion, comentario, id_lugar_turistico as id_elemento,'Lugar turístico' as tipo, (select titulo from fresh_tour.lugares_turisticos where id = cvlt.id_lugar_turistico) as elemento FROM fresh_tour.comentarios_valoracions_lugares_turisticos cvlt WHERE id_usuario = $1 UNION ALL select id, id_usuario, titulo, to_char(data, 'DD-MM-YY') as data, valoracion, comentario, id_monumento as id_elemento, 'Monumento' as tipo, (select titulo from fresh_tour.monumentos where id = cvm.id_monumento) as elemento FROM fresh_tour.comentarios_valoracions_monumentos cvm WHERE id_usuario = $1 UNION ALL select id, id_usuario, titulo, to_char(data, 'DD-MM-YY') as data, valoracion, comentario, id_planificacion as id_elemento, 'Planificacion' as tipo, (select titulo from fresh_tour.planificacions where id = cvp.id_planificacion) FROM fresh_tour.comentarios_valoracions_planificacions cvp WHERE id_usuario = $1";
sql.usuarios.get.plans = "SELECT *, 'Planificación' as tipo FROM fresh_tour.planificacions p WHERE id_usuario = $1";
sql.usuarios.get.hospedaxeFav = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, true AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1)";
sql.usuarios.get.hostalariaFav = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, true AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1)";
sql.usuarios.get.ocioFav = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, true AS favorito FROM fresh_tour.actividades_ocio ao WHERE id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1)";
sql.usuarios.get.outrasFav = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, true AS favorito FROM fresh_tour.outras_actividades oa WHERE id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1)";

sql.elementos = {};
sql.elementos.sortBy = {};
sql.elementos.sortBy.titulo = "SELECT *, 'Lugar turístico' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_turisticos_favoritos ltf WHERE ltf.id_lugar_turistico = lt.id AND ltf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_turisticos lt WHERE prezo is not NULL UNION ALL SELECT *, 'Monumento' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.monumentos_favoritos mf WHERE mf.id_monumento = m.id AND mf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.monumentos m WHERE m.prezo is not NULL ORDER BY titulo ASC"
sql.elementos.sortBy.valoracion = "SELECT *, 'Lugar turístico' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_turisticos_favoritos ltf WHERE ltf.id_lugar_turistico = lt.id AND ltf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_turisticos lt WHERE prezo is not NULL UNION ALL SELECT *, 'Monumento' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.monumentos_favoritos mf WHERE mf.id_monumento = m.id AND mf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.monumentos m WHERE m.prezo is not NULL ORDER BY valoracion DESC"
sql.elementos.updateValoracion = {};
sql.elementos.updateValoracion.mediaLugares = "SELECT avg(valoracion) as valoracion FROM fresh_tour.comentarios_valoracions_lugares_turisticos WHERE id_lugar_turistico = $1";
sql.elementos.updateValoracion.updateLugares = "UPDATE fresh_tour.lugares_turisticos SET valoracion = $1 WHERE id = $2"
sql.elementos.updateValoracion.mediaMonumentos = "SELECT avg(valoracion) as valoracion FROM fresh_tour.comentarios_valoracions_monumentos WHERE id_monumento = $1";
sql.elementos.updateValoracion.updateMonumentos = "UPDATE fresh_tour.monumentos SET valoracion = $1 WHERE id = $2"
sql.elementos.byName = "SELECT *, 'Lugar turístico' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_turisticos_favoritos ltf WHERE ltf.id_lugar_turistico = lt.id AND ltf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_turisticos lt WHERE lt.prezo is not NULL AND (lower(lt.titulo) LIKE lower($2)) UNION ALL SELECT *, 'Monumento' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.monumentos_favoritos mf WHERE mf.id_monumento = m.id AND mf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.monumentos m WHERE m.prezo is not NULL AND (lower(m.titulo) LIKE lower($2)) ORDER BY titulo ASC"
sql.elementos.favs = {};
sql.elementos.favs.sortBy = {};
sql.elementos.favs.sortBy.titulo = "SELECT *, 'Monumento' as tipo, true AS favorito FROM fresh_tour.monumentos m WHERE id IN ( SELECT id_monumento FROM fresh_tour.monumentos_favoritos mf WHERE id_usuario = $1) UNION ALL SELECT *, 'Lugar turístico' as tipo, true AS favorito FROM fresh_tour.lugares_turisticos lt WHERE id IN ( SELECT id_lugar_turistico FROM fresh_tour.lugares_turisticos_favoritos ltf WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.elementos.favs.sortBy.valoracion = "SELECT *, 'Monumento' as tipo, true AS favorito FROM fresh_tour.monumentos m WHERE id IN ( SELECT id_monumento FROM fresh_tour.monumentos_favoritos mf WHERE id_usuario = $1) UNION ALL SELECT *, 'Lugar turístico' as tipo, true AS favorito FROM fresh_tour.lugares_turisticos lt WHERE id IN ( SELECT id_lugar_turistico FROM fresh_tour.lugares_turisticos_favoritos ltf WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.elementos.favs.new = {};
sql.elementos.favs.new.lugares = "INSERT INTO fresh_tour.lugares_turisticos_favoritos (id_usuario, id_lugar_turistico) values ($1, $2)";
sql.elementos.favs.new.monumentos = "INSERT INTO fresh_tour.monumentos_favoritos (id_usuario, id_monumento) values ($1, $2)";
sql.elementos.favs.new.planificacions = "INSERT INTO fresh_tour.planificacions_favoritas (id_usuario, id_planificacion) values ($1, $2)";
sql.elementos.favs.delete = {};
sql.elementos.favs.delete.lugares = "DELETE FROM fresh_tour.lugares_turisticos_favoritos WHERE id_usuario = $1 and id_lugar_turistico = $2";
sql.elementos.favs.delete.monumentos = "DELETE FROM fresh_tour.monumentos_favoritos WHERE id_usuario = $1 and id_monumento = $2";
sql.elementos.favs.delete.planificacions = "DELETE FROM fresh_tour.planificacions_favoritas WHERE id_usuario = $1 and id_planificacion = $2";
sql.elementos.favs.all = "SELECT *, 'Monumento' as tipo, true AS favorito FROM fresh_tour.monumentos m WHERE id IN ( SELECT id_monumento FROM fresh_tour.monumentos_favoritos mf WHERE id_usuario = $1) UNION ALL SELECT *, 'Lugar turístico' as tipo, true AS favorito FROM fresh_tour.lugares_turisticos lt WHERE id IN ( SELECT id_lugar_turistico FROM fresh_tour.lugares_turisticos_favoritos ltf WHERE id_usuario = $1)";
sql.elementos.favs.byName = "SELECT *, 'Monumento' as tipo, true AS favorito FROM fresh_tour.monumentos m WHERE id IN ( SELECT id_monumento FROM fresh_tour.monumentos_favoritos mf WHERE id_usuario = $1) and (lower(titulo) like lower($2)) UNION ALL SELECT *, 'Lugar turístico' as tipo, true AS favorito FROM fresh_tour.lugares_turisticos lt WHERE id IN ( SELECT id_lugar_turistico FROM fresh_tour.lugares_turisticos_favoritos ltf WHERE id_usuario = $1) and (lower(titulo) like lower($2))";

sql.opinions = {};
sql.opinions.get = {};
sql.opinions.get.lugares = {};
sql.opinions.get.lugares.get = "select cvlt.id, id_usuario, usuario, titulo, valoracion, comentario, id_lugar_turistico as id_elemento, to_char(data, 'DD-MM-YY') as data from fresh_tour.comentarios_valoracions_lugares_turisticos cvlt inner join (select id, usuario from fresh_tour.usuarios) as usuarios on cvlt.id_usuario = usuarios.id where cvlt.id_lugar_turistico = $1 order by data asc";
sql.opinions.get.lugares.count_valoracion = "select count(id) as count, avg(valoracion) as valoracion from fresh_tour.comentarios_valoracions_lugares_turisticos where id_lugar_turistico = $1";
sql.opinions.get.monumentos = {};
sql.opinions.get.monumentos.get = "select cvm.id, id_usuario, usuario, titulo, valoracion, comentario, id_monumento as id_elemento, to_char(data, 'DD-MM-YY') as data from fresh_tour.comentarios_valoracions_monumentos cvm inner join (select id, usuario from fresh_tour.usuarios) as usuarios on cvm.id_usuario = usuarios.id where cvm.id_monumento = $1 order by data asc";
sql.opinions.get.monumentos.count_valoracion = "select count(id) as count, avg(valoracion) as valoracion from fresh_tour.comentarios_valoracions_monumentos where id_monumento = $1";
sql.opinions.get.planificacions = {};
sql.opinions.get.planificacions.get = "select cvp.id, id_usuario, usuario, titulo, valoracion, comentario, id_planificacion as id_elemento, to_char(data, 'DD-MM-YY') as data from fresh_tour.comentarios_valoracions_planificacions cvp inner join (select id, usuario from fresh_tour.usuarios) as usuarios on cvp.id_usuario = usuarios.id where cvp.id_planificacion = $1 order by data asc";
sql.opinions.get.planificacions.count_valoracion = "select count(id) as count, avg(valoracion) as valoracion from fresh_tour.comentarios_valoracions_planificacions where id_planificacion = $1";
sql.opinions.get.hospedaxe = {};
sql.opinions.get.hospedaxe.get = "select cvlh.id, id_usuario, usuario, titulo, valoracion, comentario, id_lugar_hospedaxe as id_elemento, to_char(data, 'DD-MM-YY') as data from fresh_tour.comentarios_valoracions_lugares_hospedaxe cvlh inner join (select id, usuario from fresh_tour.usuarios) as usuarios on cvlh.id_usuario = usuarios.id where cvlh.id_lugar_hospedaxe = $1 order by data asc";
sql.opinions.get.hospedaxe.count_valoracion = "select count(id) as count, avg(valoracion) as valoracion from fresh_tour.comentarios_valoracions_lugares_hospedaxe where id_lugar_hospedaxe = $1";
sql.opinions.get.hostalaria = {};
sql.opinions.get.hostalaria.get = "select cvlh.id, id_usuario, usuario, titulo, valoracion, comentario, id_lugar_hostalaria as id_elemento, to_char(data, 'DD-MM-YY') as data from fresh_tour.comentarios_valoracions_lugares_hostalaria cvlh inner join (select id, usuario from fresh_tour.usuarios) as usuarios on cvlh.id_usuario = usuarios.id where cvlh.id_lugar_hostalaria = $1 order by data asc";
sql.opinions.get.hostalaria.count_valoracion = "select count(id) as count, avg(valoracion) as valoracion from fresh_tour.comentarios_valoracions_lugares_hostalaria where id_lugar_hostalaria = $1";
sql.opinions.get.ocio = {};
sql.opinions.get.ocio.get = "select cvlh.id, id_usuario, usuario, titulo, valoracion, comentario, id_actividade_ocio as id_elemento, to_char(data, 'DD-MM-YY') as data from fresh_tour.comentarios_valoracions_actividades_ocio cvlh inner join (select id, usuario from fresh_tour.usuarios) as usuarios on cvlh.id_usuario = usuarios.id where cvlh.id_actividade_ocio = $1 order by data asc";
sql.opinions.get.ocio.count_valoracion = "select count(id) as count, avg(valoracion) as valoracion from fresh_tour.comentarios_valoracions_actividades_ocio where id_actividade_ocio = $1";
sql.opinions.get.outras = {};
sql.opinions.get.outras.get = "select cvlh.id, id_usuario, usuario, titulo, valoracion, comentario, id_outra_actividade as id_elemento, to_char(data, 'DD-MM-YY') as data from fresh_tour.comentarios_valoracions_outras_actividades cvlh inner join (select id, usuario from fresh_tour.usuarios) as usuarios on cvlh.id_usuario = usuarios.id where cvlh.id_outra_actividade = $1 order by data asc";
sql.opinions.get.outras.count_valoracion = "select count(id) as count, avg(valoracion) as valoracion from fresh_tour.comentarios_valoracions_outras_actividades where id_outra_actividade = $1";
sql.opinions.exists = {};
sql.opinions.exists.lugares = "SELECT * FROM fresh_tour.comentarios_valoracions_lugares_turisticos WHERE id_usuario = $1 AND id_lugar_turistico = $2";
sql.opinions.exists.monumentos = "SELECT * FROM fresh_tour.comentarios_valoracions_monumentos WHERE id_usuario = $1 AND id_monumento = $2";
sql.opinions.exists.planificacions = "SELECT * FROM fresh_tour.comentarios_valoracions_planificacions WHERE id_usuario = $1 AND id_planificacion = $2";
sql.opinions.exists.hospedaxes = "SELECT * FROM fresh_tour.comentarios_valoracions_lugares_hospedaxe WHERE id_usuario = $1 AND id_lugar_hospedaxe = $2";
sql.opinions.exists.hostalaria = "SELECT * FROM fresh_tour.comentarios_valoracions_lugares_hostalaria WHERE id_usuario = $1 AND id_lugar_hostalaria = $2";
sql.opinions.exists.ocio = "SELECT * FROM fresh_tour.comentarios_valoracions_actividades_ocio WHERE id_usuario = $1 AND id_actividade_ocio = $2";
sql.opinions.exists.outras = "SELECT * FROM fresh_tour.comentarios_valoracions_outras_actividades WHERE id_usuario = $1 AND id_outra_actividade = $2";
sql.opinions.new = {};
sql.opinions.new.lugares = {};
sql.opinions.new.lugares.insert = "INSERT INTO fresh_tour.comentarios_valoracions_lugares_turisticos(id_usuario, titulo, data, valoracion, comentario, id_lugar_turistico) values ($1, $2, CURRENT_TIMESTAMP, $3, $4, $5) RETURNING id, id_usuario, titulo, to_char(data, 'DD-MM-YY') as data, valoracion, comentario, id_lugar_turistico";
sql.opinions.new.lugares.media = "SELECT avg(valoracion) as valoracion FROM fresh_tour.comentarios_valoracions_lugares_turisticos WHERE id_lugar_turistico = $1";
sql.opinions.new.lugares.updateVal = "UPDATE fresh_tour.lugares_turisticos SET valoracion = $1 WHERE id = $2 RETURNING valoracion";
sql.opinions.new.monumentos = {};
sql.opinions.new.monumentos.insert = "INSERT INTO fresh_tour.comentarios_valoracions_monumentos(id_usuario, titulo, data, valoracion, comentario, id_monumento) values ($1, $2, CURRENT_TIMESTAMP, $3, $4, $5) RETURNING id, id_usuario, titulo, to_char(data, 'DD-MM-YY') as data, valoracion, comentario, id_monumento"
sql.opinions.new.monumentos.media = "SELECT avg(valoracion) as valoracion FROM fresh_tour.comentarios_valoracions_monumentos WHERE id_monumento = $1";
sql.opinions.new.monumentos.updateVal = "UPDATE fresh_tour.monumentos SET valoracion = $1 WHERE id = $2 RETURNING valoracion";
sql.opinions.new.planificacions = {};
sql.opinions.new.planificacions.insert = "INSERT INTO fresh_tour.comentarios_valoracions_planificacions(id_usuario, titulo, data, valoracion, comentario, id_planificacion) values ($1, $2, CURRENT_TIMESTAMP, $3, $4, $5) RETURNING id, id_usuario, titulo, to_char(data, 'DD-MM-YY') as data, valoracion, comentario, id_planificacion";
sql.opinions.new.planificacions.media = "SELECT avg(valoracion) as valoracion FROM fresh_tour.comentarios_valoracions_planificacions WHERE id_planificacion = $1";
sql.opinions.new.planificacions.updateVal = "UPDATE fresh_tour.planificacions SET valoracion = $1 WHERE id = $2 RETURNING valoracion";
sql.opinions.new.hospedaxe = {};
sql.opinions.new.hospedaxe.insert = "INSERT INTO fresh_tour.hospedaxe(id_usuario, titulo, data, valoracion, comentario, id_lugar_hospedaxe) values ($1, $2, CURRENT_TIMESTAMP, $3, $4, $5) RETURNING id, id_usuario, titulo, to_char(data, 'DD-MM-YY') as data, valoracion, comentario, id_lugar_hospedaxe";
sql.opinions.new.hospedaxe.media = "SELECT avg(valoracion) as valoracion FROM fresh_tour.comentarios_valoracions_lugares_hostalaria WHERE id_lugar_hostalaria = $1";
sql.opinions.new.hospedaxe.updateVal = "UPDATE fresh_tour.lugares_hostalaria SET valoracion = $1 WHERE id = $2 RETURNING valoracion";
sql.opinions.new.hostalaria = {};
sql.opinions.new.hostalaria.insert = "INSERT INTO fresh_tour.comentarios_valoracions_lugares_hostalaria(id_usuario, titulo, data, valoracion, comentario, id_lugar_hostalaria) values ($1, $2, CURRENT_TIMESTAMP, $3, $4, $5) RETURNING id, id_usuario, titulo, to_char(data, 'DD-MM-YY') as data, valoracion, comentario, id_lugar_hostalaria";
sql.opinions.new.hostalaria.media = "SELECT avg(valoracion) as valoracion FROM fresh_tour.comentarios_valoracions_lugares_hostalaria WHERE id_lugar_hostalaria = $1";
sql.opinions.new.hostalaria.updateVal = "UPDATE fresh_tour.lugares_hostalaria SET valoracion = $1 WHERE id = $2 RETURNING valoracion";
sql.opinions.new.ocio = {};
sql.opinions.new.ocio.insert = "INSERT INTO fresh_tour.comentarios_valoracions_actividades_ocio(id_usuario, titulo, data, valoracion, comentario, id_actividade_ocio) values ($1, $2, CURRENT_TIMESTAMP, $3, $4, $5) RETURNING id, id_usuario, titulo, to_char(data, 'DD-MM-YY') as data, valoracion, comentario, id_actividade_ocio";
sql.opinions.new.ocio.media = "SELECT avg(valoracion) as valoracion FROM fresh_tour.comentarios_valoracions_actividades_ocio WHERE id_actividade_ocio = $1";
sql.opinions.new.ocio.updateVal = "UPDATE fresh_tour.actividades_ocio SET valoracion = $1 WHERE id = $2 RETURNING valoracion";
sql.opinions.new.outras = {};
sql.opinions.new.outras.insert = "INSERT INTO fresh_tour.comentarios_valoracions_outras_actividades(id_usuario, titulo, data, valoracion, comentario, id_outra_actividade) values ($1, $2, CURRENT_TIMESTAMP, $3, $4, $5) RETURNING id, id_usuario, titulo, to_char(data, 'DD-MM-YY') as data, valoracion, comentario, id_outra_actividade";
sql.opinions.new.outras.media = "SELECT avg(valoracion) as valoracion FROM fresh_tour.comentarios_valoracions_outras_actividades WHERE id_outra_actividade = $1";
sql.opinions.new.outras.updateVal = "UPDATE fresh_tour.outras_actividades SET valoracion = $1 WHERE id = $2 RETURNING valoracion";
sql.opinions.delete = {};
sql.opinions.delete.lugares = "DELETE FROM fresh_tour.comentarios_valoracions_lugares_turisticos WHERE id = $1";
sql.opinions.delete.monumentos = "DELETE FROM fresh_tour.comentarios_valoracions_monumentos WHERE id = $1";
sql.opinions.delete.planificacions = "DELETE FROM fresh_tour.comentarios_valoracions_planificacions WHERE id = $1";
sql.opinions.delete.hostalaria = "DELETE FROM fresh_tour.comentarios_valoracions_lugares_hostalaria WHERE id = $1";
sql.opinions.delete.ocio = "DELETE FROM fresh_tour.comentarios_valoracions_actividades_ocio WHERE id = $1";
sql.opinions.delete.outras = "DELETE FROM fresh_tour.comentarios_valoracions_outras_actividades WHERE id = $1";
sql.opinions.edit = {};
sql.opinions.edit.lugares = "UPDATE fresh_tour.comentarios_valoracions_lugares_turisticos SET valoracion = $1, titulo = $2, comentario = $3 WHERE id = $4";
sql.opinions.edit.monumentos = "UPDATE fresh_tour.comentarios_valoracions_monumentos SET valoracion = $1, titulo = $2, comentario = $3 WHERE id = $4";
sql.opinions.edit.planificacions = "UPDATE fresh_tour.comentarios_valoracions_planificacions SET valoracion = $1, titulo = $2, comentario = $3 WHERE id = $4";
sql.opinions.edit.hospedaxes = "UPDATE fresh_tour.comentarios_valoracions_lugares_hospedaxe SET valoracion = $1, titulo = $2, comentario = $3 WHERE id = $4";
sql.opinions.edit.hostalaria = "UPDATE fresh_tour.comentarios_valoracions_lugares_hostalaria SET valoracion = $1, titulo = $2, comentario = $3 WHERE id = $4";
sql.opinions.edit.ocio = "UPDATE fresh_tour.comentarios_valoracions_actividades_ocio SET valoracion = $1, titulo = $2, comentario = $3 WHERE id = $4";
sql.opinions.edit.outras = "UPDATE fresh_tour.comentarios_valoracions_outras_actividades SET valoracion = $1, titulo = $2, comentario = $3 WHERE id = $4";

sql.hospedaxe = {};
sql.hospedaxe.all = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh ORDER BY valoracion DESC";
sql.hospedaxe.byName = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE titulo is not null and lower(titulo) LIKE lower($2) ORDER BY valoracion DESC";
sql.hospedaxe.hoteis = {};
sql.hospedaxe.hoteis.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'hotel' ORDER BY titulo ASC";
sql.hospedaxe.hoteis.valoracon = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'hotel' ORDER BY valoracion DESC";
sql.hospedaxe.hostais = {};
sql.hospedaxe.hostais.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'hostel' ORDER BY titulo ASC";
sql.hospedaxe.hostais.valoracon = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'hostel' ORDER BY valoracion DESC";
sql.hospedaxe.aloxamento = {};
sql.hospedaxe.aloxamento.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'guest_house' ORDER BY titulo ASC";
sql.hospedaxe.aloxamento.valoracon = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'guest_house' ORDER BY valoracion DESC";
sql.hospedaxe.caravanas = {};
sql.hospedaxe.caravanas.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'caravan_site' ORDER BY titulo ASC";
sql.hospedaxe.caravanas.valoracon = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'caravan_site' ORDER BY valoracion DESC";
sql.hospedaxe.vivendas = {};
sql.hospedaxe.vivendas.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'apartment' ORDER BY titulo ASC";
sql.hospedaxe.vivendas.valoracon = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'apartment' ORDER BY valoracion DESC";
sql.hospedaxe.camping = {};
sql.hospedaxe.camping.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'camp_pitch' or lh.sub_tag LIKE 'camp_site' ORDER BY titulo ASC";
sql.hospedaxe.camping.valoracon = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'camp_pitch' or lh.sub_tag LIKE 'camp_site' ORDER BY valoracion DESC";
sql.hospedaxe.moteis = {};
sql.hospedaxe.moteis.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'motel' ORDER BY titulo ASC";
sql.hospedaxe.moteis.valoracon = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'motel' ORDER BY valoracion DESC";
sql.hospedaxe.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh ORDER BY titulo ASC";
sql.hospedaxe.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh ORDER BY valoracion DESC";
sql.hospedaxe.fav = {};
sql.hospedaxe.fav.add = "INSERT INTO fresh_tour.lugares_hospedaxe_favoritos (id_lugar_hospedaxe, id_usuario) VALUES ($1, $2)";
sql.hospedaxe.fav.delete = "DELETE FROM fresh_tour.lugares_hospedaxe_favoritos WHERE id_lugar_hospedaxe = $1 and id_usuario = $2";
sql.hospedaxe.fav.byName = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, true AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) AND lower(titulo) like lower($2)";
sql.hospedaxe.fav.hoteis = {};
sql.hospedaxe.fav.hoteis.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'hotel' and id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.hospedaxe.fav.hoteis.valoracon = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'hotel' and id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.hospedaxe.fav.hostais = {};
sql.hospedaxe.fav.hostais.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'hostel' and id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.hospedaxe.fav.hostais.valoracon = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'hostel' and id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.hospedaxe.fav.aloxamento = {};
sql.hospedaxe.fav.aloxamento.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'guest_house' and id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.hospedaxe.fav.aloxamento.valoracon = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'guest_house' and id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.hospedaxe.fav.caravanas = {};
sql.hospedaxe.fav.caravanas.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'caravan_site' and id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.hospedaxe.fav.caravanas.valoracon = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'caravan_site' and id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.hospedaxe.fav.vivendas = {};
sql.hospedaxe.fav.vivendas.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'apartment' and id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.hospedaxe.fav.vivendas.valoracon = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'apartment' and id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.hospedaxe.fav.camping = {};
sql.hospedaxe.fav.camping.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'camp_pitch' or lh.sub_tag LIKE 'camp_site' and id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.hospedaxe.fav.camping.valoracon = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'camp_pitch' or lh.sub_tag LIKE 'camp_site' and id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.hospedaxe.fav.moteis = {};
sql.hospedaxe.fav.moteis.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'motel' and id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.hospedaxe.fav.moteis.valoracon = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE lh.sub_tag LIKE 'motel' and id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.hospedaxe.fav.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.hospedaxe.fav.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hospedaxe' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE lhf.id_lugar_hospedaxe = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hospedaxe lh WHERE id IN ( SELECT id_lugar_hospedaxe FROM fresh_tour.lugares_hospedaxe_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC";

sql.lecer = {};
sql.lecer.hostalaria = {};
sql.lecer.hostalaria.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.byName = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE titulo is not null and lower(titulo) LIKE lower($2) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.fav = {};
sql.lecer.hostalaria.fav.add = "INSERT INTO fresh_tour.lugares_hostalaria_favoritos (id_lugar_hostalaria, id_usuario) VALUES ($1, $2)";
sql.lecer.hostalaria.fav.delete = "DELETE FROM fresh_tour.lugares_hostalaria_favoritos WHERE id_lugar_hostalaria = $1 and id_usuario = $2";
sql.lecer.hostalaria.fav.byName = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, true AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) AND lower(titulo) like lower($2) LIMIT 50";
sql.lecer.hostalaria.fav.bares = {};
sql.lecer.hostalaria.fav.bares.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaria' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostaleria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE lh.sub_tag LIKE 'bares' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.fav.bares.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaria' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostaleria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE lh.sub_tag LIKE 'bares' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.fav.restaurantes = {};
sql.lecer.hostalaria.fav.restaurantes.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaria' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostaleria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE lh.sub_tag LIKE 'restaurant' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.fav.restaurantes.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaria' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostaleria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE lh.sub_tag LIKE 'restaurant' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion ASC LIMIT 50";
sql.lecer.hostalaria.fav.cafes = {};
sql.lecer.hostalaria.fav.cafes.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'cafe' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.fav.cafes.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'cafe' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.fav.pubs = {};
sql.lecer.hostalaria.fav.pubs.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'pub' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.fav.pubs.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'pub' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.fav.zonas_comida = {};
sql.lecer.hostalaria.fav.zonas_comida.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'food_court' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.fav.zonas_comida.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'food_court' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.fav.comida_rapida = {};
sql.lecer.hostalaria.fav.comida_rapida.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'fast_food' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.fav.comida_rapida.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'fast_food' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.fav.xeaderias = {};
sql.lecer.hostalaria.fav.xeaderias.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'ice_cream' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.fav.xeaderias.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'ice_cream' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.fav.pastelerias = {};
sql.lecer.hostalaria.fav.pastelerias.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'confectionery' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.fav.pastelerias.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'confectionery' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.fav.panaderias = {};
sql.lecer.hostalaria.fav.panaderias.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'bakery' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.fav.panaderias.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'bakery' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.fav.chocolaterias = {};
sql.lecer.hostalaria.fav.chocolaterias.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'chocolate' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.fav.chocolaterias.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'chocolate' and id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.fav.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.fav.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE id IN ( SELECT id_lugar_hostalaria FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.bares = {};
sql.lecer.hostalaria.bares.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'bar' ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.bares.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'bar' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.restaurantes = {};
sql.lecer.hostalaria.restaurantes.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'restaurant' ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.restaurantes.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'restaurant' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.cafes = {};
sql.lecer.hostalaria.cafes.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'cafe' ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.cafes.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'cafe' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.pubs = {};
sql.lecer.hostalaria.pubs.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'pub' ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.pubs.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'pub' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.zonas_comida = {};
sql.lecer.hostalaria.zonas_comida.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'food_court' ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.zonas_comida.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'food_court' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.comida_rapida = {};
sql.lecer.hostalaria.comida_rapida.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'fast_food' ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.comida_rapida.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'fast_food' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.xeaderias = {};
sql.lecer.hostalaria.xeaderias.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'ice_cream' ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.xeaderias.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'ice_cream' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.pastelerias = {};
sql.lecer.hostalaria.pastelerias.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'confectionery' ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.pastelerias.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'confectionery' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.panaderias = {};
sql.lecer.hostalaria.panaderias.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'bakery' ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.panaderias.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'bakery' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.hostalaria.chocolaterias = {};
sql.lecer.hostalaria.chocolaterias.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'chocolate' ORDER BY titulo ASC LIMIT 50";
sql.lecer.hostalaria.chocolaterias.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Hostalaría' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.lugares_hostalaria_favoritos lhf WHERE lhf.id_lugar_hostalaria = lh.id AND lhf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.lugares_hostalaria lh WHERE sub_tag LIKE 'chocolate' ORDER BY valoracion DESC LIMIT 50";

sql.lecer.ocio = {};
sql.lecer.ocio.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao ORDER BY valoracion DESC";
sql.lecer.ocio.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao ORDER BY titulo ASC";
sql.lecer.ocio.byName = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.acitividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE titulo is not null and lower(titulo) LIKE lower($2) ORDER BY valoracion DESC";
sql.lecer.ocio.fav = {};
sql.lecer.ocio.fav.add = "INSERT INTO fresh_tour.actividades_ocio_favoritas (id_actividade_ocio, id_usuario) VALUES ($1, $2)";
sql.lecer.ocio.fav.delete = "DELETE FROM fresh_tour.actividadeS_ocio_favoritas WHERE id_actividade_ocio = $1 and id_usuario = $2";
sql.lecer.ocio.fav.byName = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, true AS favorito FROM fresh_tour.actividades_ocio ao WHERE id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) AND lower(titulo) like lower($2)";
sql.lecer.ocio.fav.picnic = {};
sql.lecer.ocio.fav.picnic.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE (sub_tag LIKE 'picnic_table' or sub_tag LIKE 'picnic_site') and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.picnic.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE (sub_tag LIKE 'picnic_table' or sub_tag LIKE 'picnic_site') and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.arcade = {};
sql.lecer.ocio.fav.arcade.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'amusement_arcade' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.arcade.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'amusement_arcade' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.bolera = {};
sql.lecer.ocio.fav.bolera.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'bowling_alley' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.bolera.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'bowling_alley' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.escape_room = {};
sql.lecer.ocio.fav.escape_room.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'escape_game' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.escape_room.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'escape_game' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.xardin = {};
sql.lecer.ocio.fav.xardin.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'garden' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.xardin.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'garden' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.parque = {};
sql.lecer.ocio.fav.parque.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'park' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.parque.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'park' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.parque_infantil = {};
sql.lecer.ocio.fav.parque_infantil.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'playground' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.parque_infantil.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'playground' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.estadio = {};
sql.lecer.ocio.fav.estadio.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'stadium' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.estadio.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'stadium' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.elasticas = {};
sql.lecer.ocio.fav.elasticas.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'trampoline_park' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.elasticas.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'trampoline_park' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.pitch = {};
sql.lecer.ocio.fav.pitch.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'pitch' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.pitch.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'pitch' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.centro_deportivo = {};
sql.lecer.ocio.fav.centro_deportivo.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'sports_centre' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.centro_deportivo.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'sports_centre' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.terraza = {};
sql.lecer.ocio.fav.terraza.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'outdoor_seating' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.terraza.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'outdoor_seating' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.baile = {};
sql.lecer.ocio.fav.baile.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'dance' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.baile.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'dance' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.pabellon = {};
sql.lecer.ocio.fav.pabellon.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'sports_hall' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.pabellon.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'sports_hall' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.cine = {};
sql.lecer.ocio.fav.cine.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'cinema' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.cine.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'cinema' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.teatro = {};
sql.lecer.ocio.fav.teatro.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'theatre' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.teatro.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'theatre' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.club_nocturno = {};
sql.lecer.ocio.fav.club_nocturno.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'nightclub' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.club_nocturno.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'nightclub' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.mirador = {};
sql.lecer.ocio.fav.mirador.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'viewpoint' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.fav.mirador.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'viewpoint' and id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY valoracion DESC";
sql.lecer.ocio.fav.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE id IN ( SELECT id_actividade_ocio FROM fresh_tour.actividades_ocio_favoritas aof WHERE id_usuario = $1) ORDER BY titulo ASC";
sql.lecer.ocio.picnic = {};
sql.lecer.ocio.picnic.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'picnic_table' or sub_tag LIKE 'picnic_site' ORDER BY titulo ASC";
sql.lecer.ocio.picnic.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'picnic_table' or sub_tag LIKE 'picnic_site' ORDER BY valoracion DESC";
sql.lecer.ocio.arcade = {};
sql.lecer.ocio.arcade.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'amusement_arcade' ORDER BY titulo ASC";
sql.lecer.ocio.arcade.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'amusement_arcade' ORDER BY valoracion DESC";
sql.lecer.ocio.bolera = {};
sql.lecer.ocio.bolera.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'bowling_alley' ORDER BY titulo ASC";
sql.lecer.ocio.bolera.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'bowling_alley' ORDER BY valoracion DESC";
sql.lecer.ocio.escape_room = {};
sql.lecer.ocio.escape_room.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'escape_game' ORDER BY titulo ASC";
sql.lecer.ocio.escape_room.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'escape_game' ORDER BY valoracion DESC";
sql.lecer.ocio.xardin = {};
sql.lecer.ocio.xardin.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'garden' ORDER BY titulo ASC";
sql.lecer.ocio.xardin.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'garden' ORDER BY valoracion DESC";
sql.lecer.ocio.parque = {};
sql.lecer.ocio.parque.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'park' ORDER BY titulo ASC";
sql.lecer.ocio.parque.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'park' ORDER BY valoracion DESC";
sql.lecer.ocio.parque_infantil = {};
sql.lecer.ocio.parque_infantil.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'playground' ORDER BY titulo ASC";
sql.lecer.ocio.parque_infantil.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'playground' ORDER BY valoracion DESC";
sql.lecer.ocio.estadio = {};
sql.lecer.ocio.estadio.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'stadium' ORDER BY titulo ASC";
sql.lecer.ocio.estadio.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'stadium' ORDER BY valoracion DESC";
sql.lecer.ocio.elasticas = {};
sql.lecer.ocio.elasticas.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'trampoline_park' ORDER BY titulo ASC";
sql.lecer.ocio.elasticas.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'trampoline_park' ORDER BY valoracion DESC";
sql.lecer.ocio.pitch = {};
sql.lecer.ocio.pitch.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'pitch' ORDER BY titulo ASC";
sql.lecer.ocio.pitch.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'pitch' ORDER BY valoracion DESC";
sql.lecer.ocio.centro_deportivo = {};
sql.lecer.ocio.centro_deportivo.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'sports_centre' ORDER BY titulo ASC";
sql.lecer.ocio.centro_deportivo.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'sports_centre' ORDER BY valoracion DESC";
sql.lecer.ocio.terraza = {};
sql.lecer.ocio.terraza.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'outdoor_seating' ORDER BY titulo ASC";
sql.lecer.ocio.terraza.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'outdoor_seating' ORDER BY valoracion DESC";
sql.lecer.ocio.baile = {};
sql.lecer.ocio.baile.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'dance' ORDER BY titulo ASC";
sql.lecer.ocio.baile.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'dance' ORDER BY valoracion DESC";
sql.lecer.ocio.pabellon = {};
sql.lecer.ocio.pabellon.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'sports_hall' ORDER BY titulo ASC";
sql.lecer.ocio.pabellon.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'sports_hall' ORDER BY valoracion DESC";
sql.lecer.ocio.cine = {};
sql.lecer.ocio.cine.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'cinema' ORDER BY titulo ASC";
sql.lecer.ocio.cine.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'cinema' ORDER BY valoracion DESC";
sql.lecer.ocio.teatro = {};
sql.lecer.ocio.teatro.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'theatre' ORDER BY titulo ASC";
sql.lecer.ocio.teatro.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'theatre' ORDER BY valoracion DESC";
sql.lecer.ocio.club_nocturno = {};
sql.lecer.ocio.club_nocturno.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'nightclub' ORDER BY titulo ASC";
sql.lecer.ocio.club_nocturno.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'nightclub' ORDER BY valoracion DESC";
sql.lecer.ocio.mirador = {};
sql.lecer.ocio.mirador.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'viewpoint' ORDER BY titulo ASC";
sql.lecer.ocio.mirador.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Ocio' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.actividades_ocio_favoritas aof WHERE aof.id_actividade_ocio = ao.id AND aof.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.actividades_ocio ao WHERE sub_tag LIKE 'viewpoint' ORDER BY valoracion DESC";

sql.lecer.outras = {};
sql.lecer.outras.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.byName = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE titulo is not null and lower(titulo) LIKE lower($2) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav = {};
sql.lecer.outras.fav.add = "INSERT INTO fresh_tour.outras_actividades_favoritas (id_outra_actividade, id_usuario) VALUES ($1, $2)";
sql.lecer.outras.fav.delete = "DELETE FROM fresh_tour.outras_actividades_favoritas WHERE id_outra_actividade = $1 and id_usuario = $2";
sql.lecer.outras.fav.byName = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, true AS favorito FROM fresh_tour.outras_actividades oa WHERE id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) AND lower(titulo) like lower($2) LIMIT 50";
sql.lecer.outras.fav.bancos = {};
sql.lecer.outras.fav.bancos.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'bank' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.bancos.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'bank' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50"
sql.lecer.outras.fav.farmacias = {};
sql.lecer.outras.fav.farmacias.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'pharmacy' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.farmacias.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'pharmacy' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.correos = {};
sql.lecer.outras.fav.correos.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'post_office' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.correos.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'post_office' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.taxis = {};
sql.lecer.outras.fav.taxis.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'taxi' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.taxis.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'taxi' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.policia = {};
sql.lecer.outras.fav.policia.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'police' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.policia.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'police' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.parking_bici = {};
sql.lecer.outras.fav.parking_bici.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'bicycle_parking' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.parking_bici.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'bicycle_parking' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.caixeiro = {};
sql.lecer.outras.fav.caixeiro.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'atm' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.caixeiro.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'atm' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.baños = {};
sql.lecer.outras.fav.baños.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'toilets' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.baños.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'toilets' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.supers = {};
sql.lecer.outras.fav.supers.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'supermarket' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.supers.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'supermarket' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.supers_peque = {};
sql.lecer.outras.fav.supers_peque.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'convenience' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.supers_peque.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'convenience' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.pescaderias = {};
sql.lecer.outras.fav.pescaderias.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'seafood' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.pescaderias.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'seafood' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.carnicerias = {};
sql.lecer.outras.fav.carnicerias.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'butcher' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.carnicerias.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'butcher' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.tendas_roupa = {};
sql.lecer.outras.fav.tendas_roupa.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'clothes' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.tendas_roupa.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'clothes' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.tendas_regalos = {};
sql.lecer.outras.fav.tendas_regalos.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'gift' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.tendas_regalos.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'gift' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.tendas_zapatos = {};
sql.lecer.outras.fav.tendas_zapatos.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'shoes' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.tendas_zapatos.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'shoes' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.tendas_bebidas = {};
sql.lecer.outras.fav.tendas_bebidas.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'beverages' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.tendas_bebidas.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'beverages' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.grandes_almacenes = {};
sql.lecer.outras.fav.grandes_almacenes.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'department_store' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.grandes_almacenes.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'department_store' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.tendas_bolsas = {};
sql.lecer.outras.fav.tendas_bolsas.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'bag' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.tendas_bolsas.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'bag' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.perfumerias = {};
sql.lecer.outras.fav.perfumerias.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'perfumery' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.perfumerias.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'perfumery' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.informacion = {};
sql.lecer.outras.fav.informacion.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'information' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.informacion.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'information' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.hospitais = {};
sql.lecer.outras.fav.hospitais.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'hospital' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.hospitais.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'hospital' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.clinicas = {};
sql.lecer.outras.fav.clinicas.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'clinic' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.clinicas.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'clinic' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.tenda_fotos = {};
sql.lecer.outras.fav.tenda_fotos.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'photo' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.fav.tenda_fotos.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'photo' and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.fav.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa and id IN ( SELECT id_outra_actividade FROM fresh_tour.outras_actividades_favoritas oaf WHERE id_usuario = $1) ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.bancos = {};
sql.lecer.outras.bancos.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'bank' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.bancos.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'bank' ORDER BY valoracion DESC LIMIT 50"
sql.lecer.outras.farmacias = {};
sql.lecer.outras.farmacias.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'pharmacy' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.farmacias.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'pharmacy' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.correos = {};
sql.lecer.outras.correos.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'post_office' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.correos.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'post_office' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.taxis = {};
sql.lecer.outras.taxis.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'taxi' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.taxis.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'taxi' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.policia = {};
sql.lecer.outras.policia.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'police' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.policia.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'police' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.parking_bici = {};
sql.lecer.outras.parking_bici.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'bicycle_parking' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.parking_bici.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'bicycle_parking' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.caixeiro = {};
sql.lecer.outras.caixeiro.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'atm' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.caixeiro.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'atm' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.baños = {};
sql.lecer.outras.baños.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'toilets' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.baños.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'toilets' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.supers = {};
sql.lecer.outras.supers.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'supermarket' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.supers.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'supermarket' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.supers_peque = {};
sql.lecer.outras.supers_peque.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'convenience' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.supers_peque.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'convenience' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.pescaderias = {};
sql.lecer.outras.pescaderias.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'seafood' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.pescaderias.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'seafood' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.carnicerias = {};
sql.lecer.outras.carnicerias.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'butcher' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.carnicerias.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'butcher' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.tendas_roupa = {};
sql.lecer.outras.tendas_roupa.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'clothes' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.tendas_roupa.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'clothes' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.tendas_regalos = {};
sql.lecer.outras.tendas_regalos.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'gift' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.tendas_regalos.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'gift' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.tendas_zapatos = {};
sql.lecer.outras.tendas_zapatos.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'shoes' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.tendas_zapatos.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'shoes' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.tendas_bebidas = {};
sql.lecer.outras.tendas_bebidas.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'beverages' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.tendas_bebidas.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'beverages' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.grandes_almacenes = {};
sql.lecer.outras.grandes_almacenes.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'department_store' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.grandes_almacenes.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'department_store' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.tendas_bolsas = {};
sql.lecer.outras.tendas_bolsas.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'bag' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.tendas_bolsas.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'bag' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.perfumerias = {};
sql.lecer.outras.perfumerias.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'perfumery' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.perfumerias.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'perfumery' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.informacion = {};
sql.lecer.outras.informacion.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'information' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.informacion.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'information' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.hospitais = {};
sql.lecer.outras.hospitais.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'hospital' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.hospitais.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'hospital' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.clinicas = {};
sql.lecer.outras.clinicas.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'clinic' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.clinicas.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'clinic' ORDER BY valoracion DESC LIMIT 50";
sql.lecer.outras.tenda_fotos = {};
sql.lecer.outras.tenda_fotos.titulo = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'photo' ORDER BY titulo ASC LIMIT 50";
sql.lecer.outras.tenda_fotos.valoracion = "SELECT id, titulo, sub_tag, valoracion, 'Outra' as tipo, CASE WHEN EXISTS (SELECT 1 FROM fresh_tour.outras_actividades_favoritas oaf WHERE oaf.id_outra_actividade = oa.id AND oaf.id_usuario = $1) THEN CAST(1 AS BOOL) ELSE CAST(0 AS BOOL) END AS favorito FROM fresh_tour.outras_actividades oa WHERE sub_tag LIKE 'photo' ORDER BY valoracion DESC LIMIT 50";

module.exports = sql;